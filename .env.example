# Sepolia JSON-RPC endpoint used for read calls + as MetaMask fallback.
# Any public Sepolia RPC works; a dedicated key (Alchemy/Infura) is more reliable.
VITE_SEPOLIA_RPC_URL=https://ethereum-sepolia-rpc.publicnode.com

# Gonka testnet host (chain RPC + REST + BLS orchestrator all live on :8000).
VITE_GONKA_HOST=http://89.169.111.79:8000
